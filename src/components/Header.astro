---
import {Image} from "astro:assets"

// const userEmail = Astro.cookies.get("userEmail")
---

<header
    class="flex justify-between items-center px-24 py-12 max-sm:px-5 max-sm:py-10 max-w-7xl justify-self-center w-full mb-4 bg-cyan-500"
>
    <a href="/" class="flex gap-x-4 items-center">
        <Image
            src="/GearHeadz.png"
            alt="Gear Headz logo"
            width={64}
            height={64}
            format="webp"
            class="w-16 bg-transparent border-none rounded-full"
        />

        <p class="uppercase text-3xl text-zinc-900 font-sans font-bold">Gear Headz Unite!</p>
    </a>
    <a href="/blog">
        <button class="border border-black px-3 py-3 rounded-2xl text-white bg-slate-800">Blog</button>
    </a>
    

    <!-- {
        userEmail?.value ? (
            <a
                href="/logout"
                class="bg-teal-900 text-white py-3 px-6 rounded-xl text-xl"
            >
                Logout
            </a>
        ) : (
            <a
                href="/login"
                class="bg-teal-900 text-white py-3 px-6 rounded-xl text-xl"
            >
                Login
            </a>
        )
    } -->
</header>